# Solar System Explorer 3D

一个交互式3D太阳系模型，使用React、Three.js和React Three Fiber构建。这个项目提供了一个逼真的太阳系可视化体验，让用户可以探索行星、卫星和天体运动。
可以在[这里](https://qihuai8.github.io/solar-system-explorer-3d/)查看在线演示。

## ✨ 功能特点

- 🪐 **完整的太阳系模拟**：包含8大行星及其主要卫星
- 🌟 **精美的3D视觉效果**：高分辨率纹理、行星环、发光效果
- 🌌 **奥尔特云**：模拟太阳系边缘的天体分布
- 🕹️ **交互式控制**：
  - 鼠标拖拽旋转视角
  - 鼠标滚轮缩放
  - 时间流速控制
  - 轨道显示切换
- 📖 **行星信息**：点击行星查看详细信息
- 📱 **响应式设计**：适配不同屏幕尺寸
- 🌐 **无需Node环境运行**：支持静态部署

## 🛠️ 技术栈

- **前端框架**：React 19
- **3D图形**：Three.js
- **React 3D集成**：@react-three/fiber, @react-three/drei
- **构建工具**：Vite
- **样式**：Tailwind CSS
- **类型系统**：TypeScript
- **部署**：GitHub Pages 支持

## 📁 项目结构

```
solar-system-explorer-3d/
├── components/          # React组件
│   ├── OortCloud.tsx   # 奥尔特云组件
│   ├── Planet.tsx      # 行星组件
│   ├── SolarSystemScene.tsx  # 主场景组件
│   ├── Sun.tsx         # 太阳组件
│   └── TextureErrorBoundary.tsx  # 纹理错误边界处理
├── constants.ts        # 项目常量（行星数据等）
├── types.ts            # TypeScript类型定义
├── App.tsx             # 应用主组件
├── index.tsx           # 应用入口
├── index.html          # HTML模板
├── vite.config.ts      # Vite配置
├── package.json        # 项目依赖
└── README.md           # 项目说明
```

## 🚀 安装与启动

### 前提条件

- Node.js 16.x 或更高版本
- npm 或 yarn

### 安装步骤

1. **克隆仓库**

```bash
git clone https://github.com/your-username/solar-system-explorer-3d.git
cd solar-system-explorer-3d
```

2. **安装依赖**

```bash
npm install
# 或使用 yarn
yarn install
```

3. **启动开发服务器**

```bash
npm run dev
# 或使用 yarn
yarn dev
```

4. **在浏览器中查看**

打开浏览器，访问 http://localhost:3000

## 🏗️ 构建项目

要生成可部署的静态文件：

```bash
npm run build
# 或使用 yarn
yarn build
```

构建后的文件将生成在 `dist` 目录中。

## 🚀 部署到GitHub Pages

1. **安装gh-pages**

```bash
npm install --save-dev gh-pages
```

2. **配置Vite**

在 `vite.config.ts` 中添加base配置：

```typescript
export default defineConfig({
  // ...
  base: '/solar-system-explorer-3d/', // 使用你的GitHub仓库名称
  // ...
})
```

3. **在package.json中添加部署脚本**

```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

4. **部署项目**

```bash
npm run deploy
```

部署完成后，你的应用将在 https://your-username.github.io/solar-system-explorer-3d/ 上可用。

## 🎮 使用说明

### 基本操作

- **旋转视角**：拖拽鼠标
- **缩放**：鼠标滚轮或触摸板缩放
- **选择行星**：点击行星查看详细信息

### 控制选项

- **时间流速**：使用底部的滑块调整太阳系的运行速度
- **显示轨道**：切换底部的开关显示或隐藏行星轨道

### 行星信息面板

点击任意行星后，将显示包含以下信息的面板：
- 行星名称
- 行星描述
- 距离太阳的相对距离
- 卫星数量
- 主要卫星列表

## 🔧 自定义配置

### 添加新行星

在 `constants.ts` 文件中，你可以添加新的行星数据：

```typescript
export const PLANETS: PlanetData[] = [
  // ...
  {
    name: 'Planet Name',
    radius: 1.0,         // 相对半径
    distance: 50,        // 相对距离
    speed: 0.5,          // 轨道速度
    color: '#XXXXXX',    // 颜色代码
    rotationSpeed: 0.02, // 自转速度
    moons: [],           // 卫星列表
    description: '行星描述',
    orbitInclination: 0, // 轨道倾角（度）
    orbitAscendingNode: 0, // 升交点经度（度）
    textureUrl: '纹理图片URL'
  }
];
```

### 修改视觉效果

在各组件文件中，你可以调整：
- 纹理质量
- 发光效果
- 粒子数量（奥尔特云）
- 光照参数

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🤝 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 🌐 资源来源

- **行星纹理**：Wikimedia Commons (Solar System Scope textures)
- **3D模型**：使用Three.js生成的几何形状

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- GitHub Issues: https://github.com/chenchen8/solar-system-explorer-3d/issues
- Email: 1219982397@qq.com (主要用于技术支持和反馈)

---

感谢使用 Solar System Explorer 3D！ 🚀🌌